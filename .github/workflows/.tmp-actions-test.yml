name: Temporary Actions test

on:
  push:
    branches:
      - fix/VPNClient-shell-path-hardening

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Test Python
        run: python3 -c "print('temporary Actions test passed')"
